const banana = 5000 // VND
const apple = 10000 // VND
const orange = 7000 // VND
const getTotalAmount = (price, quantity) => price * quantity;

var amountOfPaidMoney = getTotalAmount(banana, 1);
console.log(amountOfPaidMoney);

